<Page
    x:Class="Graphics.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:view="using:TimetableDesigner.Graphics.View"
    xmlns:Model="using:TimetableDesigner.Model"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="5*"></RowDefinition>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <view:TimetableView x:Name="Timetable" ViewModel="{x:Bind ViewModel}"/>
        <view:CourseEditView x:Name="edit" timetable="{x:Bind Timetable, Mode=OneWay}" Grid.Row="2"/>
        <TextBlock x:Name="text" Text="Placeholder" FontSize="20" Grid.Row="3"/>
    </Grid>
</Page>
