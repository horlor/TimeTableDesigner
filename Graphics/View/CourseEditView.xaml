<UserControl
    x:Class="TimetableDesigner.Graphics.View.CourseEditView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TimetableDesigner.Graphics.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <TextBlock Text="Tantárgy:" Grid.Row="0"/>
        <TextBox Text="{x:Bind timetable.Selected.Subject, Mode=OneWay}" Grid.Column="1"/>
        <TextBlock Text="Tanár:" Grid.Row="1"/>
        <TextBox Text="{x:Bind timetable.Selected.Teacher, Mode=OneWay}" Grid.Row="1" Grid.Column="1"/>
        <TextBlock Text="Idő:" Grid.Row="2"/>
        <TextBox Text="{x:Bind timetable.Selected.TimeString, Mode=OneWay}" Grid.Row="2" Grid.Column="1" AllowFocusOnInteraction="True"/>
    </Grid>
</UserControl>
